source("renv/activate.R")
# print colorido - glimpse
# instalar uma vez e depois retirar do script
# install.packages(
#   "paint", 
#   repos = c(mm = "https://milesmcbain.r-universe.dev", getOption("repos"))
# )
# deixar este código no r
paint::mask_print()
